info:
  name: Ollama - List Available Running Models
  type: http
  seq: 4

http:
  method: GET
  url: "{{OLLAMA_BASE_URL}}/ps"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  List running models
  ********************
  Retrieve a list of models that are currently running

  If this does not bring any result back then run in ps: `ollama run <DOWNLOADED_MODEL_NAME>`
